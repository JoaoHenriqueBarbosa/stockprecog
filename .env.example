# brapi.dev API token — get yours (free tier available) at https://brapi.dev
# The pipeline downloads B3 prices (adjustedClose EOD) and CVM-derived fundamentals
# with YOUR OWN key. The token is never committed (.env is gitignored).
BRAPI_TOKEN=your_brapi_token_here
